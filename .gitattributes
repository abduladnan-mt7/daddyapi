# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Binary assets (none yet, but keep Git from munging them later)
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
